# 竞品舆情分析系统 — 开发日报

**日期**: 2026-07-20
**项目**: 极限战场 · 竞品舆情分析系统
**仓库**: https://github.com/Juvia-xin/s18-others-sentiment

---

## 今日完成

### 1. 调度策略调整

| 改动 | 说明 |
|------|------|
| 每日 → 每周 | 定时任务改为**每周一 9:00** 执行，自动跑前 7 天数据分析 |
| 周分析模式 | 新增 `runWeeklyAnalysis()`，遍历上周 7 天逐日采集+分析，完成后生成静态快照并存档 |

文件: `server/src/scheduler/index.js`, `server/src/scheduler/dailyTask.js`

### 2. 历史周报功能（新 Tab）

| 功能 | 说明 |
|------|------|
| 前端 Tab | 看板新增第 4 个 Tab「历史报告」 |
| 表格展示 | 生成时间 / 覆盖周期 / 查看链接 |
| 后端存档 | 新增 `report_archives` 表，API: `GET /api/reports/archives` |
| 静态快照 | 周分析完成后自动生成 HTML 报表，保存至 `server/data/snapshots/` |

文件: `server/src/scheduler/snapshotGenerator.js`, `server/src/models/reportArchive.js`, `server/src/db.js`, `server/src/routes/reports.js`, `client/src/App.jsx`, `client/src/api.js`

### 3. 周报内容结构

快照 HTML 包含三部分：

1. **S18 极限战场 · 上周舆情分析**（独立模块）
   - 5 指标卡（动态数/评论数/正面率/中性率/负面率）
   - 数据来源标签（平台分布）
   - 好评关键词 + 代表评论（5 条玩家原句）
   - 负评关键词 + 代表评论（5 条玩家原句）

2. **竞品舆情摘要**
   - 按评论量排序的紧凑卡片
   - 每竞品一行：名称 + 数据 + 风险标签 + 关键词 + 一句话小结

3. **竞品排名总览**
   - S18 蓝色高亮行 + ★ 标记
   - 风险等级列（高/中/低）

### 4. Bug 修复

| 问题 | 根因 | 修复 |
|------|------|------|
| 刷新页面数据全为空 | 前端日期默认值 `today`（2026-07-20），但数据库最新数据止于 2026-07-17 | 新增 `GET /api/reports/latest-date`，前端初始化时自动定位最新数据日期 |

---

## 当前数据状态

- 数据库最新记录: **2026-07-17**
- 竞品: 10 个（活跃）
- 平台: Steam / 微博 / B站 / TapTap / 抖音（5 个已接入），小红书 ❌ 待修复
- 周报快照: `sentiment-weekly-2026-07-17-2026-07-17.html` 已生成

---

## 启动命令

```bash
# 服务
cd server && npm start

# 手动周分析
node src/scheduler/dailyTask.js --week

# 前端构建
cd client && npm run build
```

---

## 待办

| 优先级 | 事项 |
|--------|------|
| 🔴 高 | 小红书爬虫接入修复 |
| 🔴 高 | 修复 crawler 中 comment 插入的 FOREIGN KEY 约束冲突 |
| 🟡 中 | POPO 每日推送机器人 |
| 🟡 中 | 数据勘误机制 |
| 🟡 中 | 环比趋势对比 |
